/**
 @Author -> saurabh59373
**/

#include <iostream>
#include <vector>

using namespace std;

void fast(){
    std::ios_base::sync_with_stdio(0);
    std::cin.tie(0); std::cout.tie(0);
}

// Using Brute Force

int main(){
    // fast();
    int T, n;
    cin >> T;

    while(T--){
        cin >> n;

        int Sn = ((n * n) + n) / 2;
        int arr[Sn];

        for (int i = 0; i < Sn;){
            i += k;
            k++;
        }
    }
    return 0;
}
