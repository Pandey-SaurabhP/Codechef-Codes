#include <iostream>

using namespace std;

const int M = 1e9 + 7;
unsigned long long (int n){
    int f = 1;
    for (int i = 1; i <= N; i++){
        f = (f * i) % M;
    }
    return f;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int T, N;
    int arr[1e5] = {0};

    for (int i = ; i )
    cin >> T;

    while (T--){
        cin >> N;
    }
    return 0;
}
